# GPT Pro Architect durable notes

## Current topic

- LLMWiki productization and usability is a `PLAN` review, not an implementation-conformance or release review.
- GPT Pro final verdict is `APPROVE` after accepting Codex's trust-contract challenge.
- Keep a dedicated `LLM Wiki` lifecycle tab, but execute in trust-first order: evidence rebaseline → exact approval/write binding and real `app.vault` adapter → create-only production slice → lifecycle UX → operation expansion.
- Canonical write remains preview-only until exact packet-to-target/property/bytes/hash binding is proved; edited proposals must be repacketed and reconfirmed.
- Prodigy remains authoritative for Object, Schema, human approval, and provenance.
- AutoRAG, OWNtology-Kit, and AutoRAG-Research are selective design references; their runtimes and broad data collectors are not being vendored.
- OmniRoute is optional per feature profile. The deterministic LLMWiki core must work without it.

## Known boundaries

- The current review packet includes LLMWiki trust/archive contract files and selected Prodigy contracts.
- AI provider secret-management source files, personal data, real Objects/Daily, unrelated dirty changes, and full worktree diff are excluded.
- No payment module was identified in the local project; the packet uses the approved LLMWiki/AI-provider interpretation.

## Unresolved

- Awaiting user approval to turn `.omo/drafts/prodigy-llmwiki-productization-ux.md` into the final executable plan artifact.
- No implementation authority is granted by Round 24.

## Current topic: auction-region-workspace-20260801

- Goal: 경매·지역 워크스페이스에서 노트로 이동하지 않고 카드·패널·모달 안에서 조사·비교·판단·승인을 끝내는 UX 결론을 수렴한다.
- The current external review is a `PLAN` gate only. No product code, Object/Daily write, external collection, commit, push, or release is authorized by this topic.
- Existing real-estate research is a redacted architectural input: provider/package/hash/approval boundaries remain authoritative; user-facing labels and flow are the review target.
- Auction and Region surfaces should converge on purpose-based actions, explicit read-only versus approval states, return-focus/back behavior, and bounded Phase 1 slices.
- Completed: created the requested new `Prodigy OS Making` project conversation dated 2026-08-01 and ran proposal, challenge, and final-decision turns in the same conversation.
- Transport note: superseded for this topic by the authenticated Codex in-app browser project conversation below.

## Current topic final - auction-region-workspace-20260801

- project conversation: `Prodigy OS Making`
- conversation url: `https://chatgpt.com/g/g-p-6a4b05e4c9a881919da91b28922095eb-prodigy-os-making/c/6a6dfe26-f208-83e8-b090-2ee0b085d273`
- transport: Codex in-app browser, authenticated ChatGPT UI `Pro`
- packet: `packets/packet-1.md`
- architect verdict: `REVISE` at the `PLAN` gate; implementation is not authorized
- accepted direction: one Auction surface with Korean status-based primary action; `근거 확인` read-only; `결정 요약` remains a distinct read-only reference; `판단 작성`; `선택 반영`; Region is read-only context in the Auction surface
- required correction: complete lifecycle/package-state CTA mapping, lock the three boundaries, preserve AuctionDecisionPacket outside evidence/edit/writer paths, and produce no-note desktop/mobile evidence
- excluded: product code, Object/Daily writes, external collection, new Object/schema/PRE/Memory/generic workflow layer, commit, push, release
- local responses: `responses/response-10.md`, `responses/response-11.md`, `responses/response-12.md`

## Region utilization final - 2026-08-01

- topic: `auction-region-workspace-20260801`
- architect verdict: `APPROVE` at `PLAN`, conditionally limited to Phase 1
- core definition: Region is a reusable, read-only district-level judgment context; Auction remains the judgment surface
- Phase 1: Region Explorer selection → RegionIntelligencePopup detail → `이 지역 경매 보기` → existing Auction Dashboard filter by `region_sido` and `region_sigungu`; Auction panel receives a read-only Region summary; existing Region Experience input can be opened from the Auction panel and remains Region-owned
- existing data evidence: `template_auction_region.md` and `auction-region-core.js` already define the semantic `연결 경매` Dataview filter; `RegionExplorerProjection` does not directly provide Auction rows
- Phase 2: direct Auction list inside Region Explorer, Dataview snapshot/adapter validation, Region↔Auction bidirectional navigation, stronger Region comparison UX
- exact user labels: `지역 정보 없음`, `자료 오래됨`, `자료 갱신 확인`, `검증 필요`, `일부 정보 누락`, `추가 확인 필요`, `미시 입지 확인`; normal state has no extra CTA
- protected: Region Metrics read-only, RegionIntelligencePopup full read-only detail, Region Experience ownership, AuctionSourceApprovalWriter, Auction lifecycle and judgment fields
- local responses: `responses/response-13.md`, `responses/response-14.md`, `responses/response-15.md`
- implementation status: no product code, Object/Daily writes, commit, or release performed; explicit implementation direction is still required

## Next direction final - 2026-08-02

- topic: `auction-region-next-direction-20260802`
- gate: `PLAN`
- GPT Pro final verdict: `APPROVE`
- current baseline: Auction/Region desktop flow and pinned five-provider k-skill bridge are implemented; live provider breadth remains unproven
- final order: one reachable direct-provider vertical slice with Korean failure/selection/retry UX and Region `확인할 질문` → remaining provider reality → full investigation-flow stabilization → dogfooding → Automation Readiness Gate → conditional bounded background/bulk raw-package collection
- first slice must use existing package, exact identity, hash/fingerprint, approval, and no-write boundaries; no new Object/schema/writer/lifecycle or generic provider engine
- Region role: project existing neutral facts, freshness/verification/coverage/missing state, and linked auction context into explicit follow-up questions only; never polarity, recommendation, ranking, score, risk verdict, or bid price
- live-access fallback: if credentials, source access, or proxy permission is unavailable, verify honest failed/unavailable and fixture-backed recovery UX without claiming Provider Reality success
- background automation remains deferred until provider stability, identity coverage, bounded retry/timeout, package safety, and real repeated-use need are proven
- implementation authority: none; this round produced planning records and a report only

## AI decision-support final - 2026-08-02

- topic: `auction-region-ai-decision-support-20260802`
- gate: `PLAN`
- GPT Pro final verdict: `APPROVE`
- product decision: allow an explicit `AI 판단 보조` layer; facts-only presentation is too restrictive for the user's goal
- approved output: verified facts → deterministic calculations → AI interpretation → up to three conservative/baseline/aggressive scenarios → AI suggestions → evidence limitations
- data contract: use verified current facts, approved packages, canonical Region Metrics, labeled Region Experience, and historical outcome tuples with explicit region/property cohorts; expose sample/date/freshness/coverage
- history semantics: `won` only for verified official outcomes; `lost` only when the user actually bid and comparison is possible; `skipped`/포기 remains separate; no outcome means exclude from outcome metrics
- time safety: use `analysis_as_of`; exclude the current Auction's future outcome and later observations; distinguish pre-auction analysis from post-auction review
- persistence: AI output is a derived advisory draft; user edits and explicitly approves any text/value through the existing user-judgment flow; AI never directly writes protected fields
- calculation safety: establish the canonical winning-bid-ratio denominator, deterministic cohort/statistics/scenario functions, no missing-to-zero, no range for 0–2 outcomes, and AI cannot alter numbers
- revised order: provider vertical slice → decision-support data foundation without AI calls → AI 판단 보조 MVP → provider expansion/flow stabilization → dogfooding → AI and automation readiness gates
- next packet: `AI_DECISION_SUPPORT_DATA_CONTRACT`
- implementation authority: none; no code, Object/Daily/Region writes, provider calls, runtime QA, commit, push, or release performed

## AI decision-support normative plan - 2026-08-03

- final PLAN verdict: `APPROVE`; response `responses/response-32.md`; user-facing plan `AI_DECISION_SUPPORT_PLAN_2026-08-03.md`
- product: add one `AI 판단 보조` drill-down inside the existing 판단 보드; deterministic preview works without AI; one click generates a session-only, locally validated, zero-write advisory opinion
- headline: `입찰 보류 검토`, `추가 조사 후 재판단`, `보수 시나리오 검토`, `기준 시나리오 검토`; reason, counterevidence, refs, and deterministic evidence strength required
- time contract: current-time analysis only; no arbitrary historical cutoff/backtest; current case always excluded from cohorts
- calculation: winning/appraisal ratio only; Region `auction_bid_rate_6m` remains null; exact NFC+trim property cohort; exact n>=5 for Type 7 Q25/median/Q75 competition references
- user history: market won/lost, personal lost bid gap, and personal won experience remain separate; skipped excluded from price statistics
- privacy: aggregate/redacted opaque projection by default; personal excerpt is explicit opt-in; no real case/address/path/note/raw/secret transmission
- persistence: no writer/schema/cache/telemetry; human uses existing card editing if they choose to adopt any output
- ordered implementation: pure data foundation → deterministic judgment preview → Auction-local provider resolver → one-provider reality milestone → strict AI session UI → regression/Obsidian/manual dogfooding
- deferred: backtesting, bitemporal outcome observation, aliases, persistent AI output, automatic writes/providers/background work, scores/probabilities/single bid
- authority: plan records and report only; no product/test edit, provider call, runtime QA, Object/Vault write, commit, push, or release

## External market insight review attempt - 2026-08-03

- Packet 22 tested whether a public thesis such as `전세수급지수 하나면 충분하다` should enter Auction/Region decision support.
- The exact existing ChatGPT Pro conversation was reused, but three attempts produced no substantive architect response; `responses/response-33.md` records `NOT_RECEIVED`.
- No external thesis is promoted to a deterministic Region fact or implementation plan from this failed review.
- Local source evidence supports treating the index as one survey-based pressure signal, not a sufficient standalone timing rule.

## Nationwide Region thesis direction - 2026-08-03

- Nationwide common evidence coverage is the default; analysis depth is phased rather than geographically exclusive.
- Never present `서울 / 수도권 / 부산` as peer segments because Seoul is inside the capital region.
- Flat mutually exclusive segments are `서울 / 서울 외 수도권(인천·경기) / 부산 / 기타 비수도권`; the hierarchy retains capital/non-capital parents.
- Busan is the first non-capital deep pilot, not a permanent special case. All later regions use the same coverage, user-demand, internal-heterogeneity, and real-Auction validation gates.
- Keep k-skill as a case-level research adapter. Nationwide time-series evidence belongs to a separate versioned Knowledge/Thesis layer projected read-only into Region and Auction.
- AI may state a bounded opinion with evidence, counterevidence, cohort, and invalidation conditions. It may not automatically write, bid, rank, or create a canonical score.
- Start append-only temporal snapshots now; defer a full bitemporal query/database layer until point-in-time backtesting is actually built.
- External GPT returned `수정` three times but its detailed response repeatedly truncated; `responses/response-34.md` preserves that limitation without inventing missing reasoning.

## Nationwide Region thesis final - fresh project chat - 2026-08-03

- The user explicitly requested a fresh conversation inside `Prodigy OS Making`; the new three-pass review supersedes the partial transport result for planning decisions.
- Final PLAN verdict is `APPROVE` in `responses/response-36.md`.
- Nationwide coverage means canonical geography, upper-scope context, verified available data, and honest `자료 없음`; it does not mean a complete nationwide data platform.
- Deep rollout is gated `서울+부산 → 서울 외 수도권`, not three simultaneous builds.
- Gate 1 failure stops non-Seoul capital-region depth, further region promotion, nationwide theses, AI-scope expansion, and indicator expansion.
- Initial indicators are transactions, sale-price trend, rent-price trend, jeonse ratio, unsold housing, population, households, and separate won/lost/skipped personal cohorts, all property-type scoped.
- Next non-capital region is selected after Gate 2 by actual Auction demand, official data, temporal contract, market distinctiveness, and reuse without region-specific schema or engine.

## Journal period AI final - 2026-08-03

- topic: `journal-period-ai-20260803`
- gate: `PLAN`
- project conversation: `https://chatgpt.com/g/g-p-6a4b05e4c9a881919da91b28922095eb-prodigy-os-making/c/6a701a85-795c-83ee-8aa9-288e6ce9a120`
- final verdict: `APPROVE`; response `responses/response-35.md`
- rollout: Monthly bounded Evidence projection + AI inside existing validation screen → Quarterly human foundation → Quarterly AI → Yearly human foundation → Yearly review-only AI
- sparse data: Quarterly·Yearly use `blocked` for zero/broken sources, `question_only` for one complete lower-period record, and `full_review` for two or more qualified records; Monthly keeps its existing readiness gate
- Monthly privacy/grounding: only selected-month structured Evidence projection; no raw Daily text, other-month Evidence, global Knowledge, or unrelated Object input; all supporting/counter refs must resolve to submitted Evidence IDs
- persistence: one canonical file per period with session mtime guard; no append-only revision subsystem, content hash, auto merge, AI draft file, or cache
- Yearly: review artifact only; no current Identity Lens input, diff, writer, apply button, settings write, or Morning Brief integration until a separate canonical lens PLAN exists
- protected: explicit button-only AI, deterministic-first UI, session-only AI draft, source immutability, human save/validation, no automatic Knowledge/Direction/settings writes
- implementation authority: none; no product/test/docs change, provider call, runtime QA, commit, push, or release was authorized by this round

## Monthly AI coder-ready PLAN final - 2026-08-03

- Final PLAN verdict is `APPROVE`; canonical plan is `MONTHLY_AI_IMPLEMENTATION_PLAN_2026-08-03.md`; final amendments are in `responses/response-37-monthly-ai-plan.md`.
- Repair the human Monthly flow first: load/render separation, persistent edits, summary/next direction fields, real month end, and writer-paired canonical parser.
- AI input is eligible Principles only plus selected-month structured Evidence. Response cardinality is exactly one review per eligible Principle. Ineligible Principles remain deterministic-only.
- Existing readiness is unchanged. If bounded Evidence is empty, human validation remains available but AI is disabled with zero provider calls.
- Provider schemas are advisory; local recursive exact-key/ref/cardinality validation is the fail-closed safety boundary.
- Existing canonical records hydrate exactly; unmatched entries and legacy records require explicit replacement. Target mtime prevents silent overwrite.
- Daily/Weekly source path/mtime changes block AI before provider call and require destructive confirmed reload; save may continue only through explicit human source-change acknowledgement.
- AI output is session-only and has exactly two copy operations: validation rationale and next-month direction. Decision, knowledge statement, summary, save, Candidate, and Knowledge remain human-owned.
- Shared `AIProviderService` and `WeeklyFilterCore.parseDailyEvidenceBlocks` are not edited. If implementation requires either, return to PLAN.
- Implementation authority remains absent; this round changed only architect artifacts and did not run provider/runtime QA or edit product/tests/docs.

## Journal gate and role convergence - 2026-08-03

- GPT Pro final verdict: `APPROVE`; response `responses/response-38-journal-gate-role-convergence-final.md`.
- Monthly readiness is not a screen-access gate; it remains the Human Principle Validation and Candidate gate.
- Monthly states are `blocked` for zero/broken inputs, `question_only` for usable Weekly without repeated eligible Principle, and `validation` for repeated normalized Principle across different Weekly records.
- July 2026 with three Weekly records and zero repeated normalized Principle remains usable as `question_only`; allow human summary/next-direction partial save and bounded question-only AI, but no Principle decision, validation-rationale copy, Knowledge statement, Candidate, or upstream completed status.
- Canonical role copy: Daily `오늘 무엇이 나를 변화시켰는지 기록합니다.`; Weekly `이번 주에 무엇이 반복되었고 무엇을 배웠는지 살펴봅니다.`; Monthly `이번 달의 변화가 반복된 근거로 검증되는지 확인합니다.`; Quarterly `검증된 변화와 결과를 바탕으로 지금의 방향이 맞는지 점검합니다.`; Yearly `분기별 방향과 변화를 돌아보며 내가 어떤 사람이 되어가는지 성찰합니다.`
- Quarterly and Yearly remain deterministic period navigation/history surfaces in this implementation; their AI/writer foundations require a separate approved plan.
